<?xml version="1.0" encoding="utf-8"?>
<!--
  The window shown while the process starts, before Flutter's first frame.

  ## Navy, and nothing else

  It was `?android:colorBackground` - white on a light device - handing over to a
  navy Flutter splash, so every cold start flashed white. Navy here makes the
  handover invisible.

  **The plate is deliberately not drawn here.** A layer-list can only place a
  drawable at a fixed dp size, and the design sizes the plate as 44% of the screen
  width; a 160dp plate would be right on a 360dp phone and visibly wrong on a
  411dp one, and either way it would jump the moment the responsive Flutter
  version replaced it. A plate that fades in over an unchanged ground reads as
  content arriving. A plate that resizes reads as a glitch.

  On API 31+ this file is ignored: the platform splash draws the app icon on
  `windowSplashScreenBackground`, which values-v31/styles.xml sets to the same
  navy. The mark inverting between the icon and the launch screen is intended -
  the design's reason is that a stalled launch must not look like a stalled home
  screen.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/brand_navy" />
</layer-list>
